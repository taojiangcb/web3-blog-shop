module.exports = {
  // 每行的最大长度（超过此长度的行将被换行）
  printWidth: 100,
  // 使用 2 个空格作为缩进
  tabWidth: 2,
  // 不使用制表符，使用空格进行缩进
  useTabs: false,
  // 在语句末尾添加分号
  semi: true,
  // 使用单引号替代双引号
  singleQuote: true,
  // 在多行结构中尽可能使用尾随逗号
  trailingComma: 'all',
  // 对象中的大括号内侧增加空格
  bracketSpacing: true,
  // 箭头函数的参数在能省略括号时尽量省略
  arrowParens: 'avoid',
};